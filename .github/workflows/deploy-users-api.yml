name: Deploy Users API

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'users-api/**'
  workflow_dispatch:

jobs:
  deploy-users-api:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Serverless Framework
        run: npm install -g serverless@3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Install dependencies
        run: |
          cd users-api
          npm install

      - name: Build TypeScript
        run: |
          cd users-api
          npm run build

      - name: Deploy Users API
        run: |
          cd users-api
          STAGE=${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
          serverless deploy --stage $STAGE
        env:
          JWT_SECRET: ${{ github.ref == 'refs/heads/main' && secrets.PROD_JWT_SECRET || secrets.DEV_JWT_SECRET }}
          JWT_REFRESH_SECRET: ${{ github.ref == 'refs/heads/main' && secrets.PROD_JWT_REFRESH_SECRET || secrets.DEV_JWT_REFRESH_SECRET }}

      - name: Display deployment info
        run: |
          cd users-api
          STAGE=${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
          serverless info --stage $STAGE
          echo ""
          echo "Deployment complete!"
          echo "Branch: ${{ github.ref_name }}"
          echo "Stage: $STAGE"
          echo "Domain: ${{ github.ref == 'refs/heads/main' && 'https://api.zsm7.com/users' || 'https://api.zsmproperties.com/users' }}"
